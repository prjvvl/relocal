<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pentagon.localhost_reborn">

    <application
        android:allowBackup="true"
        android:icon="@drawable/localhost"
        android:label="@string/app_name"
        android:roundIcon="@drawable/localhost"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalhostReborn">
        <activity android:name=".ProfileActivity"></activity>
        <activity android:name=".MessageActivity" />
        <activity android:name=".ProductAddActivity" />
        <activity android:name=".ServiceActivity" />
        <activity android:name=".ServiceAddActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_API_key" />
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

</manifest>